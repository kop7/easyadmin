<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}EasyAdmin App{% endblock %}</title>
        <style>
            body { font-family: Arial, sans-serif; background: #f6f7fb; }
            .login-card { max-width: 400px; margin: 100px auto; background: #fff; padding: 24px; border-radius: 12px; box-shadow: 0 8px 30px rgba(0,0,0,.08); }
            label { display: block; margin-top: 10px; font-size: 14px; }
            input { width: 100%; padding: 10px; margin-top: 4px; }
            button { margin-top: 16px; width: 100%; padding: 10px; border: 0; border-radius: 8px; background: #3b82f6; color: #fff; cursor: pointer; }
            .error { color: #b91c1c; margin: 10px 0; }
            .hint { font-size: 13px; color: #4b5563; margin-top: 8px; }
        </style>
    </head>
    <body>
    {% block body %}{% endblock %}
    </body>
</html>
